## Hono-start

Basic hono project build in type-safe API with hono, zod, OpenAPI and scalar.

### To install dependencies:

```sh
bun install
```

### To run:

```sh
bun run dev
```

open http://localhost:3003
